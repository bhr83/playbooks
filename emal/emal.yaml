---
- hosts: localhost
  gather_facts: no

  vars:
   
    email_template_id: '{%- if __location == "adc" and sandbox is defined and sandbox == false -%}
                        340
                      {%- elif __location == "govcloud" -%}
                        35
                      {%- elif  __location == "wodc" and sandbox is defined and sandbox == false -%}
                        335
                      {%- elif __location == "sandbox" -%}
                        181
                      {%- endif -%}'

  tasks:
    - debug: var=email_template_id                      